name: h9-eval
on:
  pull_request:
permissions:
  contents: read
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
      - env:
          API_KEY: ${{ secrets.API_KEY }}
        run: trusted-tool --token "$API_KEY"
