import { StrictMode } from 'react';import { createRoot } from 'react-dom/client';import { BrowserRouter } from 'react-router-dom';import App from './App';import { AuthProvider } from './auth/AuthContext';import { ScanProvider } from './state/ScanContext';import './styles/index.css';const root=document.getElementById('root');if(!root)throw new Error('BuildShield-CI frontend root element was not found.');createRoot(root).render(<StrictMode><BrowserRouter><AuthProvider><ScanProvider><App/></ScanProvider></AuthProvider></BrowserRouter></StrictMode>);
