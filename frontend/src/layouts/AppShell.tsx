import { Activity,Boxes,FileSearch,Files,GitBranch,GitCompareArrows,History,Info,LayoutDashboard,ListChecks,LogOut,Radar,ShieldCheck,SlidersHorizontal } from 'lucide-react';import { NavLink,Outlet,useNavigate } from 'react-router-dom';import { useAuth } from '../auth/AuthContext';const nav=[['Overview','/app',LayoutDashboard,true],['Scanner','/app/scanner',FileSearch,false],['Findings','/app/findings',ListChecks,false],['Policy','/app/policy',SlidersHorizontal,false],['Inventory','/app/inventory',Boxes,false],['OSV Intel','/app/vulnerability-intelligence',Radar,false],['Compare','/app/compare',GitCompareArrows,false],['History','/app/history',History,false],['Reports','/app/reports',Files,false],['CI/CD','/app/cicd',GitBranch,false],['About','/app/about',Info,false]] as const;export function AppShell(){const auth=useAuth();const navigate=useNavigate();async function signOut(){await auth.logout();navigate('/login',{replace:true})}return <div className="app-shell"><aside className="sidebar"><div className="brand"><div className="brand-mark"><ShieldCheck size={24}/></div><div><strong>BuildShield-CI</strong><span>Supply-chain security</span></div></div><nav className="navigation">{nav.map(([label,to,Icon,end])=><NavLink key={to} to={to} end={end} className={({isActive})=>isActive?'nav-link active':'nav-link'}><Icon size={18}/><span>{label}</span></NavLink>)}</nav><div className="sidebar-footer"><div className="session-chip"><span className="status-dot"/><div><strong>{auth.username??'Administrator'}</strong><span>Authenticated</span></div></div><button className="ghost-button" onClick={()=>void signOut()}><LogOut size={17}/>Sign out</button></div></aside><section className="workspace"><header className="topbar"><div><span className="eyebrow">BuildShield-CI</span><h1>Security Command Center</h1></div><div className="health-pill"><Activity size={16}/>H1 + H2 protected</div></header><main className="workspace-content"><Outlet/></main></section></div>}
