import { CheckCircle2 } from 'lucide-react';const controls=['Third-party GitHub Actions are pinned to immutable commit SHAs.','CI runs regression tests and the BuildShield security gate.','SARIF integrates with GitHub Code Scanning.','Security reports are retained as workflow artifacts.'];export function CicdPage(){return <section><div className="page-heading"><div><span className="eyebrow">CI/CD</span><h2>Pipeline security posture</h2><p>BuildShield-CI enforces its own supply-chain controls.</p></div></div><div className="panel"><h3>Verified controls</h3><div className="check-list">{controls.map(x=><div key={x}><CheckCircle2 size={17}/><span>{x}</span></div>)}</div></div></section>}
