import { useEffect,useState } from 'react';import { ApiError,getHealth } from '../api/client';import { ErrorPanel,LoadingPanel } from '../components/Ui';import type { HealthResponse } from '../types/api';export function AboutPage(){const [h,setH]=useState<HealthResponse|null>(null);const [error,setError]=useState<string|null>(null);useEffect(()=>{void getHealth().then(setH).catch(e=>setError(e instanceof ApiError?e.message:'Unable to read service health.'));},[]);if(error)return <ErrorPanel message={error}/>;if(!h)return <LoadingPanel/>;return <section><div className="page-heading"><div><span className="eyebrow">About</span><h2>{h.product}</h2><p>DevSecOps software-supply-chain security platform.</p></div></div><div className="about-card"><dl><div><dt>Version</dt><dd>{h.version}</dd></div><div><dt>API status</dt><dd>{h.status}</dd></div><div><dt>Analyzers</dt><dd>npm · Python · GitHub Actions · Dockerfile</dd></div><div><dt>Outputs</dt><dd>JSON · Markdown · HTML · SARIF</dd></div></dl></div></section>}
